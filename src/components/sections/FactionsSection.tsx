import { SectionStub } from './SectionStub';

export function FactionsSection() {
  return (
    <SectionStub
      title="Factions"
      summary="Membership, invitations, reputation, and favor for every faction."
      items={[
        'Membership editor',
        'Reputation & favor controls',
        'Invitation manager',
      ]}
    />
  );
}

