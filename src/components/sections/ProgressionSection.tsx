import { SectionStub } from './SectionStub';

export function ProgressionSection() {
  return (
    <SectionStub
      title="Progression"
      summary="Meta progression details including BitNodes, source files, and playtime."
      items={[
        'BitNode & source files',
        'Playtime tracking',
        'Exploits display',
      ]}
    />
  );
}

